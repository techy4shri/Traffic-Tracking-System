# Core FastAPI + server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ML/Computer Vision - inference only
ultralytics>=8.0.0  # YOLO models for vehicle detection
easyocr>=1.7.0      # OCR for license plates (CPU-friendly)
opencv-python-headless>=4.8.0  # Image/video processing (no GUI)
numpy>=1.24.0
Pillow>=10.0.0

# Note: torch/torchvision will be installed as ultralytics dependencies
# For CPU-only on Windows, ultralytics will pull the correct torch build
